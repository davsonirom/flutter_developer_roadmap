
import 'package:flutter/material.dart';

void main() => runApp(const CodicesSystema());

class CodicesSystema extends StatelessWidget {
  const CodicesSystema({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
